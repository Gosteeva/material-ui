{{=« »=}}
import * as React from 'react';
import { PropertyControls, ControlType } from 'framer';
import Mui«componentName» from '@material-ui/core/«componentName»';
import { Icon } from './Icon';

// Define type of property
interface Props {
«& tsInterface»
  onClick: () => void;
}

export class «componentName» extends React.Component<Props> {
  // Set default properties
  static defaultProps = {
«& defaultProps»
  };

  // Items shown in property panel
  static propertyControls: PropertyControls<Props> = {
«& propertyControls»
  };

  render() {
    const { backgroundColor, color, height, icon, imageFile, imageUrl, label, width, ...other } = this.props;

    return (
      <div
        style={{
          width,
          height,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
      {(imageFile || imageUrl) ? <Mui«componentName» src={imageFile || imageUrl} /> :
        <Mui«componentName» style={{color, backgroundColor}} {...other}>{icon === '' ? label : <Icon icon={icon} />}</Mui«componentName»>}
      </div>
    );
  }
}
