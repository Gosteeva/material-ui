{{=« »=}}
import * as React from 'react';
import { PropertyControls, ControlType } from 'framer';
import Mui«componentName» from '@material-ui/core/«componentName»';
import { Icon } from './Icon';

// Define type of property
interface Props {
«& tsInterface»
}

export class «componentName» extends React.Component<Props> {
  // Set default properties
  static defaultProps = {
«& defaultProps»
  };

  // Items shown in property panel
  static propertyControls: PropertyControls<Props> = {
«& propertyControls»
  };

  render() {
    const { height, icon, label, theme, variant, width, ...other } = this.props;

    return (
      <div
        style={{
          width,
          height,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <MuiButton {{variant}} {...other}>
            <Icon icon={icon} theme={theme} style={variant !== 'fab' ? {marginRight: 8} : {}}/>
            {variant !== 'fab' ? label : ''}
        </MuiButton>
      </div>
    );
  }
}
