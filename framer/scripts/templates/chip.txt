{{=« »=}}
import * as React from 'react';
import { PropertyControls, ControlType } from 'framer';
import Mui«componentName» from '@material-ui/core/«componentName»';
import { Icon } from "./Icon";

// Define type of property
interface Props {
«& tsInterface»
  onClick: () => void;
}

export class «componentName» extends React.Component<Props> {
  // Set default properties
  static defaultProps = {
«& defaultProps»
  };

  // Items shown in property panel
  static propertyControls: PropertyControls<Props> = {
«& propertyControls»
  };

  render() {
    const { avatar, deletable, deleteIcon, height, icon, theme, width, ...other } = this.props;

    return (
      <div
        style={{
          width,
          height,
          display: 'flex',
          alignItems: 'center',
        }}
      >
        <Mui«componentName»
          icon={<Icon icon={icon} theme={theme} />}
          onDelete = {deletable ? () => {} : undefined}
          deleteIcon={deleteIcon === '' ? undefined : <Icon icon={deleteIcon} />}
          {...other}
         />
      </div>
    );
  }
}
