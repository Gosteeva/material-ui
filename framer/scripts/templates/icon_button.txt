{{=« »=}}
import * as React from 'react';
import { PropertyControls, ControlType } from 'framer';
import MuiIconButton from '@material-ui/core/IconButton';
import MuiBadge from '@material-ui/core/Badge';
import { Icon } from './Icon';

// Define type of property
interface Props {
«& tsInterface»
  onClick?: () => void;
}

export class IconButton extends React.Component<Props> {
  // Set default properties
  static defaultProps = {
«& defaultProps»
  };

  // Items shown in property panel
  static propertyControls: PropertyControls<Props> = {
«& propertyControls»
  };

  render() {
    const { badgeColor, badgeContent, height, icon, label, width, ...other } = this.props;

    const IconBadge = badgeContent === '' ? (<Icon icon={icon} />) : (<MuiBadge badgeContent={badgeContent} color={badgeColor}>
      <Icon icon={icon} />
    </MuiBadge>);

    return (
      <div
        style={{
          width,
          height,
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        <MuiIconButton {...other}>{IconBadge}</MuiIconButton>
      </div>
    );
  }
}
